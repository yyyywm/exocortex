name: Deploy site to GitHub Pages

# NOTE: 此工作流当前为占位状态。
# Quartz 静态站点生成器待配置（受网络环境影响）。
# 配置完成后，取消下方注释并更新构建步骤。

on:
  push:
    branches: [main]
  workflow_dispatch:

# concurrency:
#   group: pages
#   cancel-in-progress: false

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: echo "Quartz deployment pending. See _system/04-发布规范.md for setup instructions."

  # TODO: 配置 Quartz 后启用以下构建步骤
  # build:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 20
  #     - name: Install Dependencies
  #       run: npm ci
  #     - name: Build Quartz
  #       run: npx quartz build
  #     - name: Upload artifact
  #       uses: actions/upload-pages-artifact@v3
  #       with:
  #         path: ./public
  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   permissions:
  #     pages: write
  #     id-token: write
  #   environment:
  #     name: github-pages
  #     url: ${{ steps.deployment.outputs.page_url }}
  #   steps:
  #     - name: Deploy to GitHub Pages
  #       id: deployment
  #       uses: actions/deploy-pages@v4
